export const getters = {
  appTitle (state) {
    return state.appTitle
  },

  getError (state) {
    return state.error
  },

  getLoading (state) {
    return state.loading
  },

  getData (state) {
    return state.data
  },

  getImageData (state) {
    return state.imageData
  }
}
